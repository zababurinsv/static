export default(function(){var __webpack_modules__={"./app.router.index.mjs":function(__unused_webpack___webpack_module__,__webpack_exports__,__webpack_require__){"use strict";eval('__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   "push": function() { return /* binding */ push; }\n/* harmony export */ });\n/* harmony import */ var history_router__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! history-router */ "./node_modules/history-router/index.js");\n\nconst router = new history_router__WEBPACK_IMPORTED_MODULE_0__.default(false)\nlet system = {}\nsystem.staticProperty = {\n    routes: undefined\n}\nlet init = async () => {\n    system.staticProperty.routes = {\n            root:\'/dex\'\n    }\n    router.when(`${system.staticProperty.routes.root}/`, ({ from, to, params, response }) => {\n        console.log(\'router0 =>\',{\n            document: document.body,\n            to: to,\n            params: params\n        })\n        return {"test":"value"}\n    })\n    router.when(`${system.staticProperty.routes.root}/:index`, ({ from, to, params, response }) => {\n        console.log(\'router1 =>\',{\n            to: to,\n            params: params\n        })\n        return {"test":"value"}\n    })\n    router.when(`${system.staticProperty.routes.root}/:index/:index`, ({ from, to, params, response }) => {\n        console.log(\'router2 =>\',{\n            to: to,\n            params: params\n        })\n        return {"test":"value"}\n    })\n    router.when(`${system.staticProperty.routes.root}/:index/:index/:index`, ({ from, to, params, response }) => {\n        console.log(\'router3 =>\',{\n            to: to,\n            params: params\n        })\n        return {"test":"value"}\n    })\n    router.when(`${system.staticProperty.routes.root}/:index/:index/:index/:index`, ({ from, to, params, response }) => {\n        console.log(\'router4 =>\',{\n            to: to,\n            params: params\n        })\n        return {"test":"value"}\n    })\n    router.when(`${system.staticProperty.routes.root}/:index/:index/:index/:index/:index`, ({ from, to, params, response }) => {\n        console.log(\'router5 =>\',{\n            to: to,\n            params: params\n        })\n        return {"test":"value"}\n    })\n    router.when(`${system.staticProperty.routes.root}/:index/:index/:index/:index/:index/:index`, ({ from, to, params, response }) => {\n        console.log(\'router6 =>\',{\n            to: to,\n            params: params\n        })\n        return {"test":"value"}\n    })\n    return true\n}\nlet push = async (path) => {\n    try {\n        (system.staticProperty.routes === undefined)\n          ? (await init(),\n            router.history.push(`${system.staticProperty.routes.root}${path}`))\n          : router.history.push(`${system.staticProperty.routes.root}${path}`);\n        return {\n            _scriptDir: "file:///home/sergey/PhpstormProjects/system/app.router.index.mjs",\n            status: \'send message\',\n            success: true,\n            message: true,\n        }\n    } catch (e) {\n        return {\n            status: \'ok\',\n            success: false,\n            message: e,\n            _scriptDir: "file:///home/sergey/PhpstormProjects/system/app.router.index.mjs"\n        }\n    }\n}\n/* harmony default export */ __webpack_exports__["default"] = ({\n    "author": "Zababurin Sergey",\n    "license": "GPL-3.0-or-later",\n    "bugs": {\n        "url": "https://github.com/zababurinsv/template/issues",\n        "mail": "s.zababurin.v@gmail.com"\n    }\n});\n\n//# sourceURL=webpack://router_index/./app.router.index.mjs?')},"./node_modules/@babel/runtime/helpers/esm/extends.js":function(__unused_webpack___webpack_module__,__webpack_exports__,__webpack_require__){"use strict";eval('__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   "default": function() { return /* binding */ _extends; }\n/* harmony export */ });\nfunction _extends() {\n  _extends = Object.assign || function (target) {\n    for (var i = 1; i < arguments.length; i++) {\n      var source = arguments[i];\n\n      for (var key in source) {\n        if (Object.prototype.hasOwnProperty.call(source, key)) {\n          target[key] = source[key];\n        }\n      }\n    }\n\n    return target;\n  };\n\n  return _extends.apply(this, arguments);\n}\n\n//# sourceURL=webpack://router_index/./node_modules/@babel/runtime/helpers/esm/extends.js?')},"./node_modules/history-router/index.js":function(__unused_webpack_module,__webpack_exports__,__webpack_require__){"use strict";eval('__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var _src_HistoryRouter__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./src/HistoryRouter */ "./node_modules/history-router/src/HistoryRouter.js");\n\n\n/* harmony default export */ __webpack_exports__["default"] = (_src_HistoryRouter__WEBPACK_IMPORTED_MODULE_0__.default);\n\n\n//# sourceURL=webpack://router_index/./node_modules/history-router/index.js?')},"./node_modules/history-router/node_modules/history/esm/history.js":function(__unused_webpack_module,__webpack_exports__,__webpack_require__){"use strict";eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"createBrowserHistory\": function() { return /* binding */ createBrowserHistory; },\n/* harmony export */   \"createHashHistory\": function() { return /* binding */ createHashHistory; },\n/* harmony export */   \"createMemoryHistory\": function() { return /* binding */ createMemoryHistory; },\n/* harmony export */   \"createLocation\": function() { return /* binding */ createLocation; },\n/* harmony export */   \"locationsAreEqual\": function() { return /* binding */ locationsAreEqual; },\n/* harmony export */   \"parsePath\": function() { return /* binding */ parsePath; },\n/* harmony export */   \"createPath\": function() { return /* binding */ createPath; }\n/* harmony export */ });\n/* harmony import */ var _babel_runtime_helpers_esm_extends__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/helpers/esm/extends */ \"./node_modules/@babel/runtime/helpers/esm/extends.js\");\n/* harmony import */ var resolve_pathname__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! resolve-pathname */ \"./node_modules/resolve-pathname/esm/resolve-pathname.js\");\n/* harmony import */ var value_equal__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! value-equal */ \"./node_modules/value-equal/esm/value-equal.js\");\n/* harmony import */ var tiny_warning__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! tiny-warning */ \"./node_modules/tiny-warning/dist/tiny-warning.esm.js\");\n/* harmony import */ var tiny_invariant__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! tiny-invariant */ \"./node_modules/tiny-invariant/dist/tiny-invariant.esm.js\");\n\n\n\n\n\n\nfunction addLeadingSlash(path) {\n  return path.charAt(0) === '/' ? path : '/' + path;\n}\nfunction stripLeadingSlash(path) {\n  return path.charAt(0) === '/' ? path.substr(1) : path;\n}\nfunction hasBasename(path, prefix) {\n  return path.toLowerCase().indexOf(prefix.toLowerCase()) === 0 && '/?#'.indexOf(path.charAt(prefix.length)) !== -1;\n}\nfunction stripBasename(path, prefix) {\n  return hasBasename(path, prefix) ? path.substr(prefix.length) : path;\n}\nfunction stripTrailingSlash(path) {\n  return path.charAt(path.length - 1) === '/' ? path.slice(0, -1) : path;\n}\nfunction parsePath(path) {\n  var pathname = path || '/';\n  var search = '';\n  var hash = '';\n  var hashIndex = pathname.indexOf('#');\n\n  if (hashIndex !== -1) {\n    hash = pathname.substr(hashIndex);\n    pathname = pathname.substr(0, hashIndex);\n  }\n\n  var searchIndex = pathname.indexOf('?');\n\n  if (searchIndex !== -1) {\n    search = pathname.substr(searchIndex);\n    pathname = pathname.substr(0, searchIndex);\n  }\n\n  return {\n    pathname: pathname,\n    search: search === '?' ? '' : search,\n    hash: hash === '#' ? '' : hash\n  };\n}\nfunction createPath(location) {\n  var pathname = location.pathname,\n      search = location.search,\n      hash = location.hash;\n  var path = pathname || '/';\n  if (search && search !== '?') path += search.charAt(0) === '?' ? search : \"?\" + search;\n  if (hash && hash !== '#') path += hash.charAt(0) === '#' ? hash : \"#\" + hash;\n  return path;\n}\n\nfunction createLocation(path, state, key, currentLocation) {\n  var location;\n\n  if (typeof path === 'string') {\n    // Two-arg form: push(path, state)\n    location = parsePath(path);\n    location.state = state;\n  } else {\n    // One-arg form: push(location)\n    location = (0,_babel_runtime_helpers_esm_extends__WEBPACK_IMPORTED_MODULE_0__.default)({}, path);\n    if (location.pathname === undefined) location.pathname = '';\n\n    if (location.search) {\n      if (location.search.charAt(0) !== '?') location.search = '?' + location.search;\n    } else {\n      location.search = '';\n    }\n\n    if (location.hash) {\n      if (location.hash.charAt(0) !== '#') location.hash = '#' + location.hash;\n    } else {\n      location.hash = '';\n    }\n\n    if (state !== undefined && location.state === undefined) location.state = state;\n  }\n\n  try {\n    location.pathname = decodeURI(location.pathname);\n  } catch (e) {\n    if (e instanceof URIError) {\n      throw new URIError('Pathname \"' + location.pathname + '\" could not be decoded. ' + 'This is likely caused by an invalid percent-encoding.');\n    } else {\n      throw e;\n    }\n  }\n\n  if (key) location.key = key;\n\n  if (currentLocation) {\n    // Resolve incomplete/relative pathname relative to current location.\n    if (!location.pathname) {\n      location.pathname = currentLocation.pathname;\n    } else if (location.pathname.charAt(0) !== '/') {\n      location.pathname = (0,resolve_pathname__WEBPACK_IMPORTED_MODULE_1__.default)(location.pathname, currentLocation.pathname);\n    }\n  } else {\n    // When there is no prior location and pathname is empty, set it to /\n    if (!location.pathname) {\n      location.pathname = '/';\n    }\n  }\n\n  return location;\n}\nfunction locationsAreEqual(a, b) {\n  return a.pathname === b.pathname && a.search === b.search && a.hash === b.hash && a.key === b.key && (0,value_equal__WEBPACK_IMPORTED_MODULE_2__.default)(a.state, b.state);\n}\n\nfunction createTransitionManager() {\n  var prompt = null;\n\n  function setPrompt(nextPrompt) {\n     true ? (0,tiny_warning__WEBPACK_IMPORTED_MODULE_3__.default)(prompt == null, 'A history supports only one prompt at a time') : 0;\n    prompt = nextPrompt;\n    return function () {\n      if (prompt === nextPrompt) prompt = null;\n    };\n  }\n\n  function confirmTransitionTo(location, action, getUserConfirmation, callback) {\n    // TODO: If another transition starts while we're still confirming\n    // the previous one, we may end up in a weird state. Figure out the\n    // best way to handle this.\n    if (prompt != null) {\n      var result = typeof prompt === 'function' ? prompt(location, action) : prompt;\n\n      if (typeof result === 'string') {\n        if (typeof getUserConfirmation === 'function') {\n          getUserConfirmation(result, callback);\n        } else {\n           true ? (0,tiny_warning__WEBPACK_IMPORTED_MODULE_3__.default)(false, 'A history needs a getUserConfirmation function in order to use a prompt message') : 0;\n          callback(true);\n        }\n      } else {\n        // Return false from a transition hook to cancel the transition.\n        callback(result !== false);\n      }\n    } else {\n      callback(true);\n    }\n  }\n\n  var listeners = [];\n\n  function appendListener(fn) {\n    var isActive = true;\n\n    function listener() {\n      if (isActive) fn.apply(void 0, arguments);\n    }\n\n    listeners.push(listener);\n    return function () {\n      isActive = false;\n      listeners = listeners.filter(function (item) {\n        return item !== listener;\n      });\n    };\n  }\n\n  function notifyListeners() {\n    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {\n      args[_key] = arguments[_key];\n    }\n\n    listeners.forEach(function (listener) {\n      return listener.apply(void 0, args);\n    });\n  }\n\n  return {\n    setPrompt: setPrompt,\n    confirmTransitionTo: confirmTransitionTo,\n    appendListener: appendListener,\n    notifyListeners: notifyListeners\n  };\n}\n\nvar canUseDOM = !!(typeof window !== 'undefined' && window.document && window.document.createElement);\nfunction getConfirmation(message, callback) {\n  callback(window.confirm(message)); // eslint-disable-line no-alert\n}\n/**\n * Returns true if the HTML5 history API is supported. Taken from Modernizr.\n *\n * https://github.com/Modernizr/Modernizr/blob/master/LICENSE\n * https://github.com/Modernizr/Modernizr/blob/master/feature-detects/history.js\n * changed to avoid false negatives for Windows Phones: https://github.com/reactjs/react-router/issues/586\n */\n\nfunction supportsHistory() {\n  var ua = window.navigator.userAgent;\n  if ((ua.indexOf('Android 2.') !== -1 || ua.indexOf('Android 4.0') !== -1) && ua.indexOf('Mobile Safari') !== -1 && ua.indexOf('Chrome') === -1 && ua.indexOf('Windows Phone') === -1) return false;\n  return window.history && 'pushState' in window.history;\n}\n/**\n * Returns true if browser fires popstate on hash change.\n * IE10 and IE11 do not.\n */\n\nfunction supportsPopStateOnHashChange() {\n  return window.navigator.userAgent.indexOf('Trident') === -1;\n}\n/**\n * Returns false if using go(n) with hash history causes a full page reload.\n */\n\nfunction supportsGoWithoutReloadUsingHash() {\n  return window.navigator.userAgent.indexOf('Firefox') === -1;\n}\n/**\n * Returns true if a given popstate event is an extraneous WebKit event.\n * Accounts for the fact that Chrome on iOS fires real popstate events\n * containing undefined state when pressing the back button.\n */\n\nfunction isExtraneousPopstateEvent(event) {\n  return event.state === undefined && navigator.userAgent.indexOf('CriOS') === -1;\n}\n\nvar PopStateEvent = 'popstate';\nvar HashChangeEvent = 'hashchange';\n\nfunction getHistoryState() {\n  try {\n    return window.history.state || {};\n  } catch (e) {\n    // IE 11 sometimes throws when accessing window.history.state\n    // See https://github.com/ReactTraining/history/pull/289\n    return {};\n  }\n}\n/**\n * Creates a history object that uses the HTML5 history API including\n * pushState, replaceState, and the popstate event.\n */\n\n\nfunction createBrowserHistory(props) {\n  if (props === void 0) {\n    props = {};\n  }\n\n  !canUseDOM ?  true ? (0,tiny_invariant__WEBPACK_IMPORTED_MODULE_4__.default)(false, 'Browser history needs a DOM') : 0 : void 0;\n  var globalHistory = window.history;\n  var canUseHistory = supportsHistory();\n  var needsHashChangeListener = !supportsPopStateOnHashChange();\n  var _props = props,\n      _props$forceRefresh = _props.forceRefresh,\n      forceRefresh = _props$forceRefresh === void 0 ? false : _props$forceRefresh,\n      _props$getUserConfirm = _props.getUserConfirmation,\n      getUserConfirmation = _props$getUserConfirm === void 0 ? getConfirmation : _props$getUserConfirm,\n      _props$keyLength = _props.keyLength,\n      keyLength = _props$keyLength === void 0 ? 6 : _props$keyLength;\n  var basename = props.basename ? stripTrailingSlash(addLeadingSlash(props.basename)) : '';\n\n  function getDOMLocation(historyState) {\n    var _ref = historyState || {},\n        key = _ref.key,\n        state = _ref.state;\n\n    var _window$location = window.location,\n        pathname = _window$location.pathname,\n        search = _window$location.search,\n        hash = _window$location.hash;\n    var path = pathname + search + hash;\n     true ? (0,tiny_warning__WEBPACK_IMPORTED_MODULE_3__.default)(!basename || hasBasename(path, basename), 'You are attempting to use a basename on a page whose URL path does not begin ' + 'with the basename. Expected path \"' + path + '\" to begin with \"' + basename + '\".') : 0;\n    if (basename) path = stripBasename(path, basename);\n    return createLocation(path, state, key);\n  }\n\n  function createKey() {\n    return Math.random().toString(36).substr(2, keyLength);\n  }\n\n  var transitionManager = createTransitionManager();\n\n  function setState(nextState) {\n    (0,_babel_runtime_helpers_esm_extends__WEBPACK_IMPORTED_MODULE_0__.default)(history, nextState);\n\n    history.length = globalHistory.length;\n    transitionManager.notifyListeners(history.location, history.action);\n  }\n\n  function handlePopState(event) {\n    // Ignore extraneous popstate events in WebKit.\n    if (isExtraneousPopstateEvent(event)) return;\n    handlePop(getDOMLocation(event.state));\n  }\n\n  function handleHashChange() {\n    handlePop(getDOMLocation(getHistoryState()));\n  }\n\n  var forceNextPop = false;\n\n  function handlePop(location) {\n    if (forceNextPop) {\n      forceNextPop = false;\n      setState();\n    } else {\n      var action = 'POP';\n      transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function (ok) {\n        if (ok) {\n          setState({\n            action: action,\n            location: location\n          });\n        } else {\n          revertPop(location);\n        }\n      });\n    }\n  }\n\n  function revertPop(fromLocation) {\n    var toLocation = history.location; // TODO: We could probably make this more reliable by\n    // keeping a list of keys we've seen in sessionStorage.\n    // Instead, we just default to 0 for keys we don't know.\n\n    var toIndex = allKeys.indexOf(toLocation.key);\n    if (toIndex === -1) toIndex = 0;\n    var fromIndex = allKeys.indexOf(fromLocation.key);\n    if (fromIndex === -1) fromIndex = 0;\n    var delta = toIndex - fromIndex;\n\n    if (delta) {\n      forceNextPop = true;\n      go(delta);\n    }\n  }\n\n  var initialLocation = getDOMLocation(getHistoryState());\n  var allKeys = [initialLocation.key]; // Public interface\n\n  function createHref(location) {\n    return basename + createPath(location);\n  }\n\n  function push(path, state) {\n     true ? (0,tiny_warning__WEBPACK_IMPORTED_MODULE_3__.default)(!(typeof path === 'object' && path.state !== undefined && state !== undefined), 'You should avoid providing a 2nd state argument to push when the 1st ' + 'argument is a location-like object that already has state; it is ignored') : 0;\n    var action = 'PUSH';\n    var location = createLocation(path, state, createKey(), history.location);\n    transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function (ok) {\n      if (!ok) return;\n      var href = createHref(location);\n      var key = location.key,\n          state = location.state;\n\n      if (canUseHistory) {\n        globalHistory.pushState({\n          key: key,\n          state: state\n        }, null, href);\n\n        if (forceRefresh) {\n          window.location.href = href;\n        } else {\n          var prevIndex = allKeys.indexOf(history.location.key);\n          var nextKeys = allKeys.slice(0, prevIndex + 1);\n          nextKeys.push(location.key);\n          allKeys = nextKeys;\n          setState({\n            action: action,\n            location: location\n          });\n        }\n      } else {\n         true ? (0,tiny_warning__WEBPACK_IMPORTED_MODULE_3__.default)(state === undefined, 'Browser history cannot push state in browsers that do not support HTML5 history') : 0;\n        window.location.href = href;\n      }\n    });\n  }\n\n  function replace(path, state) {\n     true ? (0,tiny_warning__WEBPACK_IMPORTED_MODULE_3__.default)(!(typeof path === 'object' && path.state !== undefined && state !== undefined), 'You should avoid providing a 2nd state argument to replace when the 1st ' + 'argument is a location-like object that already has state; it is ignored') : 0;\n    var action = 'REPLACE';\n    var location = createLocation(path, state, createKey(), history.location);\n    transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function (ok) {\n      if (!ok) return;\n      var href = createHref(location);\n      var key = location.key,\n          state = location.state;\n\n      if (canUseHistory) {\n        globalHistory.replaceState({\n          key: key,\n          state: state\n        }, null, href);\n\n        if (forceRefresh) {\n          window.location.replace(href);\n        } else {\n          var prevIndex = allKeys.indexOf(history.location.key);\n          if (prevIndex !== -1) allKeys[prevIndex] = location.key;\n          setState({\n            action: action,\n            location: location\n          });\n        }\n      } else {\n         true ? (0,tiny_warning__WEBPACK_IMPORTED_MODULE_3__.default)(state === undefined, 'Browser history cannot replace state in browsers that do not support HTML5 history') : 0;\n        window.location.replace(href);\n      }\n    });\n  }\n\n  function go(n) {\n    globalHistory.go(n);\n  }\n\n  function goBack() {\n    go(-1);\n  }\n\n  function goForward() {\n    go(1);\n  }\n\n  var listenerCount = 0;\n\n  function checkDOMListeners(delta) {\n    listenerCount += delta;\n\n    if (listenerCount === 1 && delta === 1) {\n      window.addEventListener(PopStateEvent, handlePopState);\n      if (needsHashChangeListener) window.addEventListener(HashChangeEvent, handleHashChange);\n    } else if (listenerCount === 0) {\n      window.removeEventListener(PopStateEvent, handlePopState);\n      if (needsHashChangeListener) window.removeEventListener(HashChangeEvent, handleHashChange);\n    }\n  }\n\n  var isBlocked = false;\n\n  function block(prompt) {\n    if (prompt === void 0) {\n      prompt = false;\n    }\n\n    var unblock = transitionManager.setPrompt(prompt);\n\n    if (!isBlocked) {\n      checkDOMListeners(1);\n      isBlocked = true;\n    }\n\n    return function () {\n      if (isBlocked) {\n        isBlocked = false;\n        checkDOMListeners(-1);\n      }\n\n      return unblock();\n    };\n  }\n\n  function listen(listener) {\n    var unlisten = transitionManager.appendListener(listener);\n    checkDOMListeners(1);\n    return function () {\n      checkDOMListeners(-1);\n      unlisten();\n    };\n  }\n\n  var history = {\n    length: globalHistory.length,\n    action: 'POP',\n    location: initialLocation,\n    createHref: createHref,\n    push: push,\n    replace: replace,\n    go: go,\n    goBack: goBack,\n    goForward: goForward,\n    block: block,\n    listen: listen\n  };\n  return history;\n}\n\nvar HashChangeEvent$1 = 'hashchange';\nvar HashPathCoders = {\n  hashbang: {\n    encodePath: function encodePath(path) {\n      return path.charAt(0) === '!' ? path : '!/' + stripLeadingSlash(path);\n    },\n    decodePath: function decodePath(path) {\n      return path.charAt(0) === '!' ? path.substr(1) : path;\n    }\n  },\n  noslash: {\n    encodePath: stripLeadingSlash,\n    decodePath: addLeadingSlash\n  },\n  slash: {\n    encodePath: addLeadingSlash,\n    decodePath: addLeadingSlash\n  }\n};\n\nfunction stripHash(url) {\n  var hashIndex = url.indexOf('#');\n  return hashIndex === -1 ? url : url.slice(0, hashIndex);\n}\n\nfunction getHashPath() {\n  // We can't use window.location.hash here because it's not\n  // consistent across browsers - Firefox will pre-decode it!\n  var href = window.location.href;\n  var hashIndex = href.indexOf('#');\n  return hashIndex === -1 ? '' : href.substring(hashIndex + 1);\n}\n\nfunction pushHashPath(path) {\n  window.location.hash = path;\n}\n\nfunction replaceHashPath(path) {\n  window.location.replace(stripHash(window.location.href) + '#' + path);\n}\n\nfunction createHashHistory(props) {\n  if (props === void 0) {\n    props = {};\n  }\n\n  !canUseDOM ?  true ? (0,tiny_invariant__WEBPACK_IMPORTED_MODULE_4__.default)(false, 'Hash history needs a DOM') : 0 : void 0;\n  var globalHistory = window.history;\n  var canGoWithoutReload = supportsGoWithoutReloadUsingHash();\n  var _props = props,\n      _props$getUserConfirm = _props.getUserConfirmation,\n      getUserConfirmation = _props$getUserConfirm === void 0 ? getConfirmation : _props$getUserConfirm,\n      _props$hashType = _props.hashType,\n      hashType = _props$hashType === void 0 ? 'slash' : _props$hashType;\n  var basename = props.basename ? stripTrailingSlash(addLeadingSlash(props.basename)) : '';\n  var _HashPathCoders$hashT = HashPathCoders[hashType],\n      encodePath = _HashPathCoders$hashT.encodePath,\n      decodePath = _HashPathCoders$hashT.decodePath;\n\n  function getDOMLocation() {\n    var path = decodePath(getHashPath());\n     true ? (0,tiny_warning__WEBPACK_IMPORTED_MODULE_3__.default)(!basename || hasBasename(path, basename), 'You are attempting to use a basename on a page whose URL path does not begin ' + 'with the basename. Expected path \"' + path + '\" to begin with \"' + basename + '\".') : 0;\n    if (basename) path = stripBasename(path, basename);\n    return createLocation(path);\n  }\n\n  var transitionManager = createTransitionManager();\n\n  function setState(nextState) {\n    (0,_babel_runtime_helpers_esm_extends__WEBPACK_IMPORTED_MODULE_0__.default)(history, nextState);\n\n    history.length = globalHistory.length;\n    transitionManager.notifyListeners(history.location, history.action);\n  }\n\n  var forceNextPop = false;\n  var ignorePath = null;\n\n  function locationsAreEqual$$1(a, b) {\n    return a.pathname === b.pathname && a.search === b.search && a.hash === b.hash;\n  }\n\n  function handleHashChange() {\n    var path = getHashPath();\n    var encodedPath = encodePath(path);\n\n    if (path !== encodedPath) {\n      // Ensure we always have a properly-encoded hash.\n      replaceHashPath(encodedPath);\n    } else {\n      var location = getDOMLocation();\n      var prevLocation = history.location;\n      if (!forceNextPop && locationsAreEqual$$1(prevLocation, location)) return; // A hashchange doesn't always == location change.\n\n      if (ignorePath === createPath(location)) return; // Ignore this change; we already setState in push/replace.\n\n      ignorePath = null;\n      handlePop(location);\n    }\n  }\n\n  function handlePop(location) {\n    if (forceNextPop) {\n      forceNextPop = false;\n      setState();\n    } else {\n      var action = 'POP';\n      transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function (ok) {\n        if (ok) {\n          setState({\n            action: action,\n            location: location\n          });\n        } else {\n          revertPop(location);\n        }\n      });\n    }\n  }\n\n  function revertPop(fromLocation) {\n    var toLocation = history.location; // TODO: We could probably make this more reliable by\n    // keeping a list of paths we've seen in sessionStorage.\n    // Instead, we just default to 0 for paths we don't know.\n\n    var toIndex = allPaths.lastIndexOf(createPath(toLocation));\n    if (toIndex === -1) toIndex = 0;\n    var fromIndex = allPaths.lastIndexOf(createPath(fromLocation));\n    if (fromIndex === -1) fromIndex = 0;\n    var delta = toIndex - fromIndex;\n\n    if (delta) {\n      forceNextPop = true;\n      go(delta);\n    }\n  } // Ensure the hash is encoded properly before doing anything else.\n\n\n  var path = getHashPath();\n  var encodedPath = encodePath(path);\n  if (path !== encodedPath) replaceHashPath(encodedPath);\n  var initialLocation = getDOMLocation();\n  var allPaths = [createPath(initialLocation)]; // Public interface\n\n  function createHref(location) {\n    var baseTag = document.querySelector('base');\n    var href = '';\n\n    if (baseTag && baseTag.getAttribute('href')) {\n      href = stripHash(window.location.href);\n    }\n\n    return href + '#' + encodePath(basename + createPath(location));\n  }\n\n  function push(path, state) {\n     true ? (0,tiny_warning__WEBPACK_IMPORTED_MODULE_3__.default)(state === undefined, 'Hash history cannot push state; it is ignored') : 0;\n    var action = 'PUSH';\n    var location = createLocation(path, undefined, undefined, history.location);\n    transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function (ok) {\n      if (!ok) return;\n      var path = createPath(location);\n      var encodedPath = encodePath(basename + path);\n      var hashChanged = getHashPath() !== encodedPath;\n\n      if (hashChanged) {\n        // We cannot tell if a hashchange was caused by a PUSH, so we'd\n        // rather setState here and ignore the hashchange. The caveat here\n        // is that other hash histories in the page will consider it a POP.\n        ignorePath = path;\n        pushHashPath(encodedPath);\n        var prevIndex = allPaths.lastIndexOf(createPath(history.location));\n        var nextPaths = allPaths.slice(0, prevIndex + 1);\n        nextPaths.push(path);\n        allPaths = nextPaths;\n        setState({\n          action: action,\n          location: location\n        });\n      } else {\n         true ? (0,tiny_warning__WEBPACK_IMPORTED_MODULE_3__.default)(false, 'Hash history cannot PUSH the same path; a new entry will not be added to the history stack') : 0;\n        setState();\n      }\n    });\n  }\n\n  function replace(path, state) {\n     true ? (0,tiny_warning__WEBPACK_IMPORTED_MODULE_3__.default)(state === undefined, 'Hash history cannot replace state; it is ignored') : 0;\n    var action = 'REPLACE';\n    var location = createLocation(path, undefined, undefined, history.location);\n    transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function (ok) {\n      if (!ok) return;\n      var path = createPath(location);\n      var encodedPath = encodePath(basename + path);\n      var hashChanged = getHashPath() !== encodedPath;\n\n      if (hashChanged) {\n        // We cannot tell if a hashchange was caused by a REPLACE, so we'd\n        // rather setState here and ignore the hashchange. The caveat here\n        // is that other hash histories in the page will consider it a POP.\n        ignorePath = path;\n        replaceHashPath(encodedPath);\n      }\n\n      var prevIndex = allPaths.indexOf(createPath(history.location));\n      if (prevIndex !== -1) allPaths[prevIndex] = path;\n      setState({\n        action: action,\n        location: location\n      });\n    });\n  }\n\n  function go(n) {\n     true ? (0,tiny_warning__WEBPACK_IMPORTED_MODULE_3__.default)(canGoWithoutReload, 'Hash history go(n) causes a full page reload in this browser') : 0;\n    globalHistory.go(n);\n  }\n\n  function goBack() {\n    go(-1);\n  }\n\n  function goForward() {\n    go(1);\n  }\n\n  var listenerCount = 0;\n\n  function checkDOMListeners(delta) {\n    listenerCount += delta;\n\n    if (listenerCount === 1 && delta === 1) {\n      window.addEventListener(HashChangeEvent$1, handleHashChange);\n    } else if (listenerCount === 0) {\n      window.removeEventListener(HashChangeEvent$1, handleHashChange);\n    }\n  }\n\n  var isBlocked = false;\n\n  function block(prompt) {\n    if (prompt === void 0) {\n      prompt = false;\n    }\n\n    var unblock = transitionManager.setPrompt(prompt);\n\n    if (!isBlocked) {\n      checkDOMListeners(1);\n      isBlocked = true;\n    }\n\n    return function () {\n      if (isBlocked) {\n        isBlocked = false;\n        checkDOMListeners(-1);\n      }\n\n      return unblock();\n    };\n  }\n\n  function listen(listener) {\n    var unlisten = transitionManager.appendListener(listener);\n    checkDOMListeners(1);\n    return function () {\n      checkDOMListeners(-1);\n      unlisten();\n    };\n  }\n\n  var history = {\n    length: globalHistory.length,\n    action: 'POP',\n    location: initialLocation,\n    createHref: createHref,\n    push: push,\n    replace: replace,\n    go: go,\n    goBack: goBack,\n    goForward: goForward,\n    block: block,\n    listen: listen\n  };\n  return history;\n}\n\nfunction clamp(n, lowerBound, upperBound) {\n  return Math.min(Math.max(n, lowerBound), upperBound);\n}\n/**\n * Creates a history object that stores locations in memory.\n */\n\n\nfunction createMemoryHistory(props) {\n  if (props === void 0) {\n    props = {};\n  }\n\n  var _props = props,\n      getUserConfirmation = _props.getUserConfirmation,\n      _props$initialEntries = _props.initialEntries,\n      initialEntries = _props$initialEntries === void 0 ? ['/'] : _props$initialEntries,\n      _props$initialIndex = _props.initialIndex,\n      initialIndex = _props$initialIndex === void 0 ? 0 : _props$initialIndex,\n      _props$keyLength = _props.keyLength,\n      keyLength = _props$keyLength === void 0 ? 6 : _props$keyLength;\n  var transitionManager = createTransitionManager();\n\n  function setState(nextState) {\n    (0,_babel_runtime_helpers_esm_extends__WEBPACK_IMPORTED_MODULE_0__.default)(history, nextState);\n\n    history.length = history.entries.length;\n    transitionManager.notifyListeners(history.location, history.action);\n  }\n\n  function createKey() {\n    return Math.random().toString(36).substr(2, keyLength);\n  }\n\n  var index = clamp(initialIndex, 0, initialEntries.length - 1);\n  var entries = initialEntries.map(function (entry) {\n    return typeof entry === 'string' ? createLocation(entry, undefined, createKey()) : createLocation(entry, undefined, entry.key || createKey());\n  }); // Public interface\n\n  var createHref = createPath;\n\n  function push(path, state) {\n     true ? (0,tiny_warning__WEBPACK_IMPORTED_MODULE_3__.default)(!(typeof path === 'object' && path.state !== undefined && state !== undefined), 'You should avoid providing a 2nd state argument to push when the 1st ' + 'argument is a location-like object that already has state; it is ignored') : 0;\n    var action = 'PUSH';\n    var location = createLocation(path, state, createKey(), history.location);\n    transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function (ok) {\n      if (!ok) return;\n      var prevIndex = history.index;\n      var nextIndex = prevIndex + 1;\n      var nextEntries = history.entries.slice(0);\n\n      if (nextEntries.length > nextIndex) {\n        nextEntries.splice(nextIndex, nextEntries.length - nextIndex, location);\n      } else {\n        nextEntries.push(location);\n      }\n\n      setState({\n        action: action,\n        location: location,\n        index: nextIndex,\n        entries: nextEntries\n      });\n    });\n  }\n\n  function replace(path, state) {\n     true ? (0,tiny_warning__WEBPACK_IMPORTED_MODULE_3__.default)(!(typeof path === 'object' && path.state !== undefined && state !== undefined), 'You should avoid providing a 2nd state argument to replace when the 1st ' + 'argument is a location-like object that already has state; it is ignored') : 0;\n    var action = 'REPLACE';\n    var location = createLocation(path, state, createKey(), history.location);\n    transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function (ok) {\n      if (!ok) return;\n      history.entries[history.index] = location;\n      setState({\n        action: action,\n        location: location\n      });\n    });\n  }\n\n  function go(n) {\n    var nextIndex = clamp(history.index + n, 0, history.entries.length - 1);\n    var action = 'POP';\n    var location = history.entries[nextIndex];\n    transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function (ok) {\n      if (ok) {\n        setState({\n          action: action,\n          location: location,\n          index: nextIndex\n        });\n      } else {\n        // Mimic the behavior of DOM histories by\n        // causing a render after a cancelled POP.\n        setState();\n      }\n    });\n  }\n\n  function goBack() {\n    go(-1);\n  }\n\n  function goForward() {\n    go(1);\n  }\n\n  function canGo(n) {\n    var nextIndex = history.index + n;\n    return nextIndex >= 0 && nextIndex < history.entries.length;\n  }\n\n  function block(prompt) {\n    if (prompt === void 0) {\n      prompt = false;\n    }\n\n    return transitionManager.setPrompt(prompt);\n  }\n\n  function listen(listener) {\n    return transitionManager.appendListener(listener);\n  }\n\n  var history = {\n    length: entries.length,\n    action: 'POP',\n    location: entries[index],\n    index: index,\n    entries: entries,\n    createHref: createHref,\n    push: push,\n    replace: replace,\n    go: go,\n    goBack: goBack,\n    goForward: goForward,\n    canGo: canGo,\n    block: block,\n    listen: listen\n  };\n  return history;\n}\n\n\n\n\n//# sourceURL=webpack://router_index/./node_modules/history-router/node_modules/history/esm/history.js?")},"./node_modules/history-router/src/HistoryRouter.js":function(__unused_webpack_module,__webpack_exports__,__webpack_require__){"use strict";eval('__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var history__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! history */ "./node_modules/history-router/node_modules/history/esm/history.js");\n/* harmony import */ var _validate__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./validate */ "./node_modules/history-router/src/validate.js");\n/* harmony import */ var _Route__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./Route */ "./node_modules/history-router/src/Route.js");\n/* harmony import */ var _Middleware__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./Middleware */ "./node_modules/history-router/src/Middleware.js");\n\n\n\n\n\n\n\nclass HistoryRouter {\n  /**\n   * Create HistoryRouter Instance\n   *\n   * @param {Boolean} hash\n   */\n  constructor (hash = true) {\n    /**\n     * @property {Object} history\n     */\n    this.history = hash ? (0,history__WEBPACK_IMPORTED_MODULE_3__.createHashHistory)() : (0,history__WEBPACK_IMPORTED_MODULE_3__.createBrowserHistory)()\n\n    /**\n     * @property {Array}\n     */\n    this.routes = []\n\n    /**\n     * @property {Array}\n     */\n    this.middlewares = []\n\n    /**\n     * history.js Blocking\n     * https://github.com/ReactTraining/history/blob/master/docs/Blocking.md\n     */\n    this.history.block(location => (0,_validate__WEBPACK_IMPORTED_MODULE_0__.default)(this.history, this.routes, this.middlewares, location, hash))\n  }\n\n  /**\n   * Register Route\n   *\n   * @param {String} path\n   * @param {Function} cb\n   * @param {Array} middlewares\n   */\n  when (path, cb, middlewares = []) {\n    for (let i = 0; i < middlewares.length; i++) {\n      middlewares[i] = new _Middleware__WEBPACK_IMPORTED_MODULE_2__.default(middlewares[i])\n    }\n    this.routes.push(new _Route__WEBPACK_IMPORTED_MODULE_1__.default(path, cb, middlewares))\n  }\n\n  /**\n   * Register global middleware\n   *\n   * @param {Function} cb\n   */\n  middleware (cb) {\n    this.middlewares.push(new _Middleware__WEBPACK_IMPORTED_MODULE_2__.default(cb))\n  }\n}\n\n/** Only for CDN, or Bower */\nwindow.HistoryRouter = HistoryRouter\n\n/* harmony default export */ __webpack_exports__["default"] = (HistoryRouter);\n\n\n//# sourceURL=webpack://router_index/./node_modules/history-router/src/HistoryRouter.js?')},"./node_modules/history-router/src/Middleware.js":function(__unused_webpack_module,__webpack_exports__,__webpack_require__){"use strict";eval('__webpack_require__.r(__webpack_exports__);\nclass Middleware {\n  /**\n   * Create Middleware Instance\n   */\n  constructor (cb) {\n    /**\n     * @property {Function}\n     */\n    this.cb = cb\n  }\n\n  /**\n   * Run middleware\n   *\n   * @param {Object} from\n   * @param {Object} to\n   * @param {*} response\n   *\n   * @return {*}\n   */\n  run (from, to, response = null) {\n    return this.cb({ from, to, response })\n  }\n}\n\n/* harmony default export */ __webpack_exports__["default"] = (Middleware);\n\n\n//# sourceURL=webpack://router_index/./node_modules/history-router/src/Middleware.js?')},"./node_modules/history-router/src/Route.js":function(__unused_webpack_module,__webpack_exports__,__webpack_require__){"use strict";eval('__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var url_composer__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! url-composer */ "./node_modules/url-composer/dist/url-composer.js");\n/* harmony import */ var url_composer__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(url_composer__WEBPACK_IMPORTED_MODULE_0__);\n\n\nclass Route {\n  /**\n   * Create Route Instance\n   *\n   * @param {String} path\n   * @param {Function} cb\n   * @param {Array} middlewares\n   */\n  constructor (path, cb, middlewares) {\n    /**\n     * @property {String}\n     */\n    this.path = path\n\n    /**\n     * @property {Function}\n     */\n    this.cb = cb\n\n    /**\n     * @property {Array}\n     */\n    this.middlewares = middlewares\n  }\n\n  /**\n   * Run route\n   *\n   * @param {Object} from\n   * @param {Object} to\n   * @param {Object} params\n   * @param {*} response\n   */\n  run (from, to, params, response) {\n    this.cb({ from, to, params, response })\n  }\n\n  /**\n   * Validate url path\n   *\n   * @param {String} path\n   * @param {String} hash\n   *\n   * @return {Boolean}\n   */\n  validate (path, hash) {\n    return url_composer__WEBPACK_IMPORTED_MODULE_0___default().test(hash\n      ? { path: `/#/${this.path}`, url: `/#/${path}` }\n      : { path: this.path, url: path }\n    )\n  }\n}\n\n/* harmony default export */ __webpack_exports__["default"] = (Route);\n\n\n//# sourceURL=webpack://router_index/./node_modules/history-router/src/Route.js?')},"./node_modules/history-router/src/validate.js":function(__unused_webpack_module,__webpack_exports__,__webpack_require__){"use strict";eval('__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var url_composer__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! url-composer */ "./node_modules/url-composer/dist/url-composer.js");\n/* harmony import */ var url_composer__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(url_composer__WEBPACK_IMPORTED_MODULE_0__);\n/* harmony import */ var _validateFromMiddlewares__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./validateFromMiddlewares */ "./node_modules/history-router/src/validateFromMiddlewares.js");\n\n\n\n/**\n * Blocking\n *\n * @param {Object} history\n * @param {Array} routes\n * @param {Array} middlewares\n * @param {Location} location\n * @param {Boolean} hash\n *\n * @return {Boolean}\n */\n/* harmony default export */ __webpack_exports__["default"] = ((history, routes, middlewares, location, hash) => {\n  const path = `${location.pathname}${location.search}${location.hash}`\n  const to = location\n  const from = history.location\n\n  for (let i = 0; i < routes.length; i++) {\n    if (routes[i].validate(path, hash)) {\n      let response = (0,_validateFromMiddlewares__WEBPACK_IMPORTED_MODULE_1__.default)(middlewares, from, to)\n      if ((routes[i].middlewares.length > 0) && response) {\n        response = (0,_validateFromMiddlewares__WEBPACK_IMPORTED_MODULE_1__.default)(routes[i].middlewares, from, to, response)\n      }\n      if (response) {\n        const params = url_composer__WEBPACK_IMPORTED_MODULE_0___default().parse({ path, definition: routes[i].path, object: true })\n        return routes[i].run(from, to, params, response)\n      }\n      return false\n    }\n  }\n  return true\n});\n\n\n//# sourceURL=webpack://router_index/./node_modules/history-router/src/validate.js?')},"./node_modules/history-router/src/validateFromMiddlewares.js":function(__unused_webpack_module,__webpack_exports__,__webpack_require__){"use strict";eval('__webpack_require__.r(__webpack_exports__);\n/**\n * Validate from middlewares\n *\n * @param {Array} middlewares\n * @param {Object} from\n * @param {Object} to\n * @param {*} response\n */\n/* harmony default export */ __webpack_exports__["default"] = ((middlewares, from, to, response = null) => {\n  if (middlewares.length > 0) {\n    for (let i = 0; i < middlewares.length; i++) {\n      response = middlewares[i].run(from, to, response)\n      if (!response) return false\n    }\n    return response\n  }\n  return true\n});\n\n\n//# sourceURL=webpack://router_index/./node_modules/history-router/src/validateFromMiddlewares.js?')},"./node_modules/resolve-pathname/esm/resolve-pathname.js":function(__unused_webpack_module,__webpack_exports__,__webpack_require__){"use strict";eval("__webpack_require__.r(__webpack_exports__);\nfunction isAbsolute(pathname) {\n  return pathname.charAt(0) === '/';\n}\n\n// About 1.5x faster than the two-arg version of Array#splice()\nfunction spliceOne(list, index) {\n  for (var i = index, k = i + 1, n = list.length; k < n; i += 1, k += 1) {\n    list[i] = list[k];\n  }\n\n  list.pop();\n}\n\n// This implementation is based heavily on node's url.parse\nfunction resolvePathname(to, from) {\n  if (from === undefined) from = '';\n\n  var toParts = (to && to.split('/')) || [];\n  var fromParts = (from && from.split('/')) || [];\n\n  var isToAbs = to && isAbsolute(to);\n  var isFromAbs = from && isAbsolute(from);\n  var mustEndAbs = isToAbs || isFromAbs;\n\n  if (to && isAbsolute(to)) {\n    // to is absolute\n    fromParts = toParts;\n  } else if (toParts.length) {\n    // to is relative, drop the filename\n    fromParts.pop();\n    fromParts = fromParts.concat(toParts);\n  }\n\n  if (!fromParts.length) return '/';\n\n  var hasTrailingSlash;\n  if (fromParts.length) {\n    var last = fromParts[fromParts.length - 1];\n    hasTrailingSlash = last === '.' || last === '..' || last === '';\n  } else {\n    hasTrailingSlash = false;\n  }\n\n  var up = 0;\n  for (var i = fromParts.length; i >= 0; i--) {\n    var part = fromParts[i];\n\n    if (part === '.') {\n      spliceOne(fromParts, i);\n    } else if (part === '..') {\n      spliceOne(fromParts, i);\n      up++;\n    } else if (up) {\n      spliceOne(fromParts, i);\n      up--;\n    }\n  }\n\n  if (!mustEndAbs) for (; up--; up) fromParts.unshift('..');\n\n  if (\n    mustEndAbs &&\n    fromParts[0] !== '' &&\n    (!fromParts[0] || !isAbsolute(fromParts[0]))\n  )\n    fromParts.unshift('');\n\n  var result = fromParts.join('/');\n\n  if (hasTrailingSlash && result.substr(-1) !== '/') result += '/';\n\n  return result;\n}\n\n/* harmony default export */ __webpack_exports__[\"default\"] = (resolvePathname);\n\n\n//# sourceURL=webpack://router_index/./node_modules/resolve-pathname/esm/resolve-pathname.js?")},"./node_modules/tiny-invariant/dist/tiny-invariant.esm.js":function(__unused_webpack_module,__webpack_exports__,__webpack_require__){"use strict";eval("__webpack_require__.r(__webpack_exports__);\nvar isProduction = \"development\" === 'production';\nvar prefix = 'Invariant failed';\nfunction invariant(condition, message) {\n    if (condition) {\n        return;\n    }\n    if (isProduction) {\n        throw new Error(prefix);\n    }\n    throw new Error(prefix + \": \" + (message || ''));\n}\n\n/* harmony default export */ __webpack_exports__[\"default\"] = (invariant);\n\n\n//# sourceURL=webpack://router_index/./node_modules/tiny-invariant/dist/tiny-invariant.esm.js?")},"./node_modules/tiny-warning/dist/tiny-warning.esm.js":function(__unused_webpack_module,__webpack_exports__,__webpack_require__){"use strict";eval('__webpack_require__.r(__webpack_exports__);\nvar isProduction = "development" === \'production\';\nfunction warning(condition, message) {\n  if (!isProduction) {\n    if (condition) {\n      return;\n    }\n\n    var text = "Warning: " + message;\n\n    if (typeof console !== \'undefined\') {\n      console.warn(text);\n    }\n\n    try {\n      throw Error(text);\n    } catch (x) {}\n  }\n}\n\n/* harmony default export */ __webpack_exports__["default"] = (warning);\n\n\n//# sourceURL=webpack://router_index/./node_modules/tiny-warning/dist/tiny-warning.esm.js?')},"./node_modules/url-composer/dist/url-composer.js":function(module){eval("(function (global, factory) {\n\t true ? module.exports = factory() :\n\t0;\n}(this, (function () { 'use strict';\n\n/**\n * @module url-composer\n * @description Module to build dynamic URLs without a fuss\n */\n\n//\n// Path analysis regular expressions\n//\n\n/**\n * Trailing slash regular expression\n * @private\n */\nvar TRAILING_SLASH = /\\/$/;\n/**\n * Leading slash regular expression\n * @private\n */\nvar LEADING_SLASH = /^\\//;\n/**\n * Parentheses global regular expression\n * @private\n */\nvar PARENTHESES = /[()]/g;\n/**\n * Optional parameters global regular expression\n * @private\n */\nvar OPTIONAL_PARAMS = /\\((.*?)\\)/g;\n/**\n * Splat parameters global regular expression\n * @private\n */\nvar SPLAT_PARAMS = /\\*\\w+/g;\n/**\n * Named parameter regular expression\n * @private\n */\nvar NAMED_PARAM = /(\\(\\?)?:\\w+/;\n/**\n * Named parameters global regular expression\n * @private\n */\nvar NAMED_PARAMS = /(\\(\\?)?:\\w+/g;\n/**\n * Some wierd escape regular expression\n * @private\n */\nvar ESCAPE = /[-{}[\\]+?.,\\\\^$|#\\s]/g;\n\n//\n// Helper functions\n//\n\n/**\n * Checks if a given object is an array\n *\n * @private\n *\n * @param  {mixed} obj The object to check\n * @return {boolean}   `true` if `obj` is an array else `false`\n */\nfunction isArray (obj) {\n  return Object.prototype.toString.call(obj) === '[object Array]'\n}\n\n/**\n * Check if a given object is empty\n *\n * @private\n *\n * @param  {object} obj The object to check\n * @return {boolean}    `true` if `obj` is empty else `false`\n */\nfunction isEmpty (obj) {\n  if (obj == null) { return true }\n  if (obj.length > 0) { return false }\n  if (obj.length === 0) { return true }\n  if (typeof obj !== 'object') { return true }\n\n  for (var key in obj) {\n    if (hasOwnProperty.call(obj, key)) { return false }\n  }\n\n  return true\n}\n\n//\n// ## Path parsing functions\n//\n\n/**\n * Inject arguments into a dynamic path definition and clean unused optional parts\n *\n * @private\n *\n * @param  {string} path The dynamic path definition\n * @param  {mixed}  args Object or array of arguments to inject.\n *                       If it is an array, arguments will be injected in sequential order.\n *                       For an object, the object keys will be used to map values to the dynamic parts of the path.\n * @return {string}      The parsed path with injected arguments\n */\nfunction composePath (path, args) {\n  path = path || '';\n  args = args || [];\n\n  if (isEmpty(args)) {\n    return removeOptionalParams(path)\n  }\n\n  path = replaceArgs(path, args);\n\n  return removeTrailingSlash(\n    removeParentheses(path)\n  )\n}\n\n/**\n * Replace dynamic parts of a path by given values\n *\n * @private\n *\n * @param  {string} path The dynamic path definition\n * @param  {mixed}  args Object or array of arguments to inject.\n *                       If it is an array, arguments will be injected in sequential order.\n *                       For an object, the object keys will be used to map values to the dynamic parts of the path.\n * @return {string}      Path with injected arguments\n */\nfunction replaceArgs (path, args) {\n  args = args || [];\n\n  if (!isArray(args)) {\n    var paramNames = path.match(NAMED_PARAMS);\n    if (paramNames) {\n      args = paramNames.map(function (name) { return args[name.substr(1)]; });\n    }\n  }\n\n  if (isArray(args)) {\n    args.forEach(function (arg) {\n      if (arg) { path = replaceArg(path, arg); }\n    });\n  }\n\n  var matches = path.match(OPTIONAL_PARAMS);\n\n  if (matches) {\n    matches.forEach(function (part) {\n      if (isNamedOrSplatParam(part)) {\n        path = path.replace(part, '');\n      }\n    });\n  }\n\n  return path\n}\n\n/**\n * Replace the first matching dynamic part of a path by the given argument\n *\n * @private\n *\n * @param  {string} path The dynamic path definition\n * @param  {mixed}  arg  The value to inject\n * @return {string}      The modified path\n */\nfunction replaceArg (path, arg) {\n  var hasNamedParam = path.indexOf(':') !== -1;\n  arg = encodeURIComponent(arg);\n\n  if (hasNamedParam) {\n    return path.replace(NAMED_PARAM, arg)\n  }\n\n  return path.replace(SPLAT_PARAMS, arg)\n}\n\n/**\n * Check if the next dynamic part in a path is a named or splat parameter definition\n *\n * @private\n *\n * @param  {string} param Dynamic part of a dynamic path definition\n * @return {boolean}      `true` if `param` is a named or splat parameter else `false`\n */\nfunction isNamedOrSplatParam (param) {\n  return NAMED_PARAM.test(param) || SPLAT_PARAMS.test(param)\n}\n\n/**\n * Strip the unfilled optional parameters from a dynamic path definition\n *\n * @private\n *\n * @param  {string} path The dynamic path to modify\n * @return {string}      The modified path\n */\nfunction removeOptionalParams (path) {\n  return path.replace(OPTIONAL_PARAMS, '')\n}\n\n/**\n * Remove the last character from a path if it is a slash\n *\n * @private\n *\n * @param  {string} path The path to modify\n * @return {string}      The modified path\n */\nfunction removeTrailingSlash (path) {\n  return path.replace(TRAILING_SLASH, '')\n}\n\n/**\n * Remove the first character from a path if it is a slash\n *\n * @private\n *\n * @param  {string} path The path to modify\n * @return {string}      The modified path\n */\nfunction removeLeadingSlash (path) {\n  return path.replace(LEADING_SLASH, '')\n}\n\n/**\n * Remove/clean remaining parentheses from a path after it has been parsed\n *\n * @private\n *\n * @param  {string} path The path to modify/clean\n * @return {string}      The modified path\n */\nfunction removeParentheses (path) {\n  return path.replace(PARENTHESES, '')\n}\n\n/**\n * Smart concatenation of host, path, query and hash. Will add the correct glue character when needed\n *\n * @private\n *\n * @param  {object} options Object describing the url\n * @return {string}         Concatenation of host, path, query and hash\n */\nfunction smartConcat (ref) {\n  var host = ref.host;\n  var path = ref.path;\n  var query = ref.query;\n  var hash = ref.hash;\n\n  // Normalize host\n  host = removeTrailingSlash(host);\n\n  // Split path in case path contains an existing query\n  var ref$1 = path.split('?');\n  var pathPart = ref$1[0];\n  var queryPart = ref$1[1];\n\n  // Normalize path\n  path = removeTrailingSlash(\n    removeLeadingSlash(pathPart)\n  );\n\n  // Add specific glue characters\n  path = path ? (\"/\" + path) : '';\n  queryPart = queryPart ? (queryPart + \"&\") : '';\n  query = query ? (\"?\" + queryPart + query) : '';\n  hash = hash ? (\"#\" + hash) : '';\n\n  return (\"\" + host + path + query + hash)\n}\n\n/**\n * Test the existence of certain fields in the stats\n *\n * @private\n *\n * @param  {array}  params List of analyzed parameters\n * @param  {string} field  Name of the field to analyze\n * @return {array}         Filtered array\n */\nfunction testParameter (params, field) {\n  var result = [];\n\n  for (var i = 0; i < params.length; i++) {\n    var p = params[i];\n    if (p[field] && p.value === '') {\n      result.push(p);\n    }\n  }\n\n  return result\n}\n\n//\n// ## Public API functions\n//\n\n/**\n * Retrieve matches for named and splat params for a dynamic path definition\n *\n * @name match\n * @function\n * @public\n *\n * @param  {string} path Dynamic path definition\n * @return {object}      Object with a `named` and `splat` array containing the extracted parameter names\n */\nfunction getParamsMatch (path) {\n  return {\n    named: (path && path.match(NAMED_PARAMS)) || [],\n    splat: (path && path.match(SPLAT_PARAMS)) || []\n  }\n}\n\n/**\n * Transform a dynamic path definition to an executable regular expression\n *\n * @name regex\n * @function\n * @public\n *\n * @param  {string} route The route/path to transform\n * @return {RegExp}       The resulting regular expression instance\n */\nfunction routeToRegex (route) {\n  route = route.replace(ESCAPE, '\\\\$&')\n    .replace(OPTIONAL_PARAMS, '(?:$1)?')\n    .replace(NAMED_PARAMS, function (match, optional) { return optional ? match : '([^/?]+)'; })\n    .replace(SPLAT_PARAMS, '([^?]*?)');\n\n  return new RegExp('^' + route + '(?:\\\\?([\\\\s\\\\S]*))?$')\n}\n\n/**\n * Build the path part of a URL using dynamic path definitions\n *\n * @name path\n * @function\n * @public\n *\n * @param  {object} options An object containing `path` and `params` keys which will be used to build the resulting path.\n * @return {string}         The built path\n */\nfunction buildPath (options) {\n  options = options || {};\n\n  return composePath(options.path, options.params)\n}\n\n/**\n * Build the query part of a URL\n *\n * @name query\n * @function\n * @public\n *\n * @param  {object} options An object containing a `query` key. The `key` should be an object of key/value pairs that\n *                          will be converted to a URL query string.\n * @return {string}         Encoded URL query string\n */\nfunction buildQuery (options) {\n  var query = [];\n  options = options || {};\n\n  for (var key in options.query) {\n    var param = options.query[key];\n    query.push((key + \"=\" + (encodeURIComponent(param))));\n  }\n\n  return query.length ? query.join('&') : ''\n}\n\n/**\n * Test a URL against a dynamic path definition\n *\n * @public\n *\n * @param  {object} options An object with `path` and `url` keys.\n *                          The `path` is the dynamic path definition against which the `url` will be tested\n * @return {boolean}        `true` if `url` matches the `path` else `false`\n */\nfunction test (options) {\n  options = options || {};\n\n  var re = routeToRegex(options.path);\n\n  return re.test(options.url)\n}\n\n/**\n * Build a complete URL\n *\n * @public\n *\n * @param  {object} options An object containing `host`, `path`, `params`, `query` and `hash`.\n *                          Everything is optional, calling `build` without any parameters will just return an empty string.\n * @return {string}         The built URL\n */\nfunction build (options) {\n  options = options || {};\n  options.host = options.host || '';\n\n  var path = buildPath(options);\n  var query = buildQuery(options);\n\n  return smartConcat({ host: options.host, path: path, query: query, hash: options.hash })\n}\n\n/**\n * Transform an arguments array into an object using the dynamic path definition\n *\n * @name params\n * @function\n * @public\n *\n * @param  {string} path The dynamic path definition\n * @param  {array}  args Arguments array\n * @return {object}      The resulting key/value pairs\n */\nfunction paramsArray2Object (path, args) {\n  var result = {};\n  var params = getParamsMatch(path);\n\n  var i = 0;\n\n  params.named.forEach(compose);\n  params.splat.forEach(compose);\n\n  return result\n\n  // Helper\n\n  function compose (name) {\n    result[name.slice(1)] = args[i++];\n  }\n}\n\n/**\n * Generate stats about a path\n *\n * @public\n *\n * @param  {string} path Dynamic path definition\n * @param  {object} args Object of arguments to analyze the state of path if it was injected with the given parameters\n * @return {object}      Object containing different stats about the path\n */\nfunction stats (path, args) {\n  var optional = path.match(OPTIONAL_PARAMS) || [];\n  var ref = getParamsMatch(path);\n  var named = ref.named;\n  var splat = ref.splat;\n\n  var params = named.concat(splat);\n\n  args = args || {};\n\n  if (isArray(args)) {\n    args = paramsArray2Object(path, args);\n  }\n\n  params = params.map(function (param) {\n    var isOptional = false;\n\n    for (var i = 0; i < optional.length; i++) {\n      var p = optional[i];\n      if (p.indexOf(param) !== -1) {\n        isOptional = true;\n        break\n      }\n    }\n\n    return {\n      name: param,\n      value: args[param.slice(1)] || '',\n      optional: isOptional,\n      required: !isOptional\n    }\n  });\n\n  return {\n    params: params,\n    hasOptionalParams: OPTIONAL_PARAMS.test(path),\n    missingOptionalParams: testParameter(params, 'optional'),\n    missingRequiredParams: testParameter(params, 'required'),\n    missingParams: testParameter(params, 'name')\n  }\n}\n\n/**\n * Parse a given url `path` according to its `definition` to extract the parameters\n *\n * @public\n *\n * @throws {Error} Throws if `options` object is missing `path` or `definition`\n *\n * @param  {object} options Object containing a `path` and dynamic path `definition`.\n *                          Can optionnaly take `object: true` to convert the result to an object, defaults to `false`.\n * @return {mixed}          Array of parameter values extracted from the path or key/value pair object.\n *                          Return `null` if the `path` does not match the `definition`.\n */\nfunction parse (options) {\n  if ( options === void 0 ) options = {};\n\n  var path = options.path;\n  var definition = options.definition;\n  var object = options.object;\n\n  if (!path && !definition) {\n    throw new Error('url-composer: Missing path and definition')\n  }\n\n  var re = routeToRegex(definition);\n\n  var params = re.exec(path);\n\n  if (!params) { return null }\n\n  params = params.slice(1);\n\n  var result = params.map(function (param, i) {\n    if (i === params.length - 1) { return param || null }\n    return param ? decodeURIComponent(param) : null\n  });\n\n  if (object) {\n    var query = result.pop();\n    result = paramsArray2Object(definition, result);\n    result.query = query;\n  }\n\n  return result\n}\n\nvar index = {\n  build: build,\n  test: test,\n  stats: stats,\n  parse: parse,\n  params: paramsArray2Object,\n  path: buildPath,\n  query: buildQuery,\n  regex: routeToRegex,\n  match: getParamsMatch\n};\n\nreturn index;\n\n})));\n\n\n//# sourceURL=webpack://router_index/./node_modules/url-composer/dist/url-composer.js?")},"./node_modules/value-equal/esm/value-equal.js":function(__unused_webpack_module,__webpack_exports__,__webpack_require__){"use strict";eval("__webpack_require__.r(__webpack_exports__);\nfunction valueOf(obj) {\n  return obj.valueOf ? obj.valueOf() : Object.prototype.valueOf.call(obj);\n}\n\nfunction valueEqual(a, b) {\n  // Test for strict equality first.\n  if (a === b) return true;\n\n  // Otherwise, if either of them == null they are not equal.\n  if (a == null || b == null) return false;\n\n  if (Array.isArray(a)) {\n    return (\n      Array.isArray(b) &&\n      a.length === b.length &&\n      a.every(function(item, index) {\n        return valueEqual(item, b[index]);\n      })\n    );\n  }\n\n  if (typeof a === 'object' || typeof b === 'object') {\n    var aValue = valueOf(a);\n    var bValue = valueOf(b);\n\n    if (aValue !== a || bValue !== b) return valueEqual(aValue, bValue);\n\n    return Object.keys(Object.assign({}, a, b)).every(function(key) {\n      return valueEqual(a[key], b[key]);\n    });\n  }\n\n  return false;\n}\n\n/* harmony default export */ __webpack_exports__[\"default\"] = (valueEqual);\n\n\n//# sourceURL=webpack://router_index/./node_modules/value-equal/esm/value-equal.js?")}},__webpack_module_cache__={};function __webpack_require__(n){if(__webpack_module_cache__[n])return __webpack_module_cache__[n].exports;var e=__webpack_module_cache__[n]={exports:{}};return __webpack_modules__[n].call(e.exports,e,e.exports,__webpack_require__),e.exports}return __webpack_require__.n=function(n){var e=n&&n.__esModule?function(){return n.default}:function(){return n};return __webpack_require__.d(e,{a:e}),e},__webpack_require__.d=function(n,e){for(var t in e)__webpack_require__.o(e,t)&&!__webpack_require__.o(n,t)&&Object.defineProperty(n,t,{enumerable:!0,get:e[t]})},__webpack_require__.o=function(n,e){return Object.prototype.hasOwnProperty.call(n,e)},__webpack_require__.r=function(n){"undefined"!=typeof Symbol&&Symbol.toStringTag&&Object.defineProperty(n,Symbol.toStringTag,{value:"Module"}),Object.defineProperty(n,"__esModule",{value:!0})},__webpack_require__("./app.router.index.mjs")}());