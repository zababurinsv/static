
export default (view, property, color, substrate, relation)=>{
    return new Promise(async (resolve, reject) => {
        switch (relation) {
            case '"orderbook"':

                break
            default:
                break
        }
    })
}