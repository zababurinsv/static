import store from '/static/html/components/component_modules/store/index.mjs'

export default (self, admin) => {
  return new Promise(async (resolve, reject) => {
    console.log('auction link')

  })
}